// Americano scheduling — fixed individual scoring with a full round-robin
// partner rotation, so every player partners with every other player exactly
// once (when player count permits). Returns a precomputed schedule of rounds.
export function generateAmericanoSchedule(): unknown {
  throw new Error("not implemented");
}
